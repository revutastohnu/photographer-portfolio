# Налаштування Telegram бота для сповіщень

Цей гід допоможе тобі налаштувати Telegram бота, який буде відправляти сповіщення про нові бронювання та успішні оплати.

## Крок 1: Створення бота через BotFather

1. **Відкрий Telegram** і знайди [@BotFather](https://t.me/botfather)
2. **Почни розмову** з BotFather і відправ команду:
   ```
   /newbot
   ```
3. **Дай назву боту** (наприклад, "Alina Photo Bookings")
4. **Дай username боту** (має закінчуватись на "bot", наприклад, `alina_photo_bot`)
5. **Збережи токен** - BotFather надішле тобі токен у форматі:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

## Крок 2: Отримання свого Chat ID

### Варіант 1: Через браузер

1. **Знайди свого бота** в Telegram і відправ йому `/start`
2. **Відкрий URL** у браузері (замість `YOUR_BOT_TOKEN` вставте токен з Кроку 1):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
3. **Знайди chat_id** в JSON відповіді:
   ```json
   {
     "ok": true,
     "result": [
       {
         "message": {
           "chat": {
             "id": 123456789,  // <-- Це твій chat_id
             "first_name": "Твоє ім'я",
             "type": "private"
           }
         }
       }
     ]
   }
   ```

### Варіант 2: Через @userinfobot

1. Знайди бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Відправ йому `/start`
3. Він відповість з твоїм `Id:` - це і є твій chat_id

## Крок 3: Додавання змінних оточення

Відкрий файл `.env.local` і додай:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Примітка:** Якщо файлу `.env.local` ще немає, скопіюй `.env.local.example`:
```bash
cp .env.local.example .env.local
```

## Крок 4: Тестування

1. **Перезапусти сервер** (якщо він запущений):
   ```bash
   npm run dev
   ```

2. **Створи тестове бронювання** через форму на сайті

3. **Перевір Telegram** - ти маєш отримати 2 повідомлення:
   - ⏳ Одразу після створення бронювання (статус "Очікує оплати")
   - ✅ Після успішної оплати (статус "Підтверджено та оплачено")

## Формат сповіщень

### При створенні бронювання:
```
🎉 Нове бронювання фотосесії!

👤 Клієнт: Ім'я клієнта
📧 Email: email@example.com
📱 Телефон: +380 xx xxx xx xx
📸 Тип зйомки: Портрет

📅 Дата: понеділок, 10 лютого 2026
⏰ Час: 14:00

💰 Сума: 2000 грн
⏳ Статус: Очікує оплати

💭 Нотатка:
Хочу фотосесію в парку
```

### При успішній оплаті:
```
💳 Оплату підтверджено!

👤 Ім'я клієнта
📸 Портрет
📅 10 лютого о 14:00
💰 2000 грн

✅ Бронювання підтверджено!
```

## Налаштування на Railway (Production)

Якщо ти деплоїш на Railway:

1. Відкрий **Railway Dashboard** → твій проект
2. Перейди в **Variables**
3. Додай змінні:
   - `TELEGRAM_BOT_TOKEN`: твій токен бота
   - `TELEGRAM_CHAT_ID`: твій chat_id

4. **Redeploy** сервіс для застосування змін

## Усунення несправностей

### Не приходять повідомлення

1. **Перевір токен**: Переконайся, що `TELEGRAM_BOT_TOKEN` правильний
2. **Перевір chat_id**: Переконайся, що ти відправив `/start` боту
3. **Перевір логи**:
   ```bash
   # Локально
   Дивись консоль де запущений npm run dev

   # На Railway
   Railway Dashboard → твій проект → Deployments → View Logs
   ```

4. **Тестовий запит**: Відправ тестове повідомлення через curl:
   ```bash
   curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage" \
     -H "Content-Type: application/json" \
     -d '{
       "chat_id": "<YOUR_CHAT_ID>",
       "text": "Test message"
     }'
   ```

### Помилка "Unauthorized"

- Токен бота неправильний або застарілий
- Створи нового бота через BotFather

### Помилка "Chat not found"

- Chat ID неправильний
- Переконайся, що ти відправив `/start` боту перед тим як отримувати chat_id

## Додаткові можливості

### Відправка в групу або канал

Якщо хочеш отримувати сповіщення в групу:

1. Додай бота в групу
2. Зроби його адміністратором
3. Отримай group chat_id (він буде негативним, наприклад `-1001234567890`)
4. Використай цей ID як `TELEGRAM_CHAT_ID`

### Кастомізація повідомлень

Відредагуй файл `lib/telegram.ts` для зміни формату повідомлень.

## Підтримка

Якщо щось не працює - перевір:
- ✅ Бот створений через BotFather
- ✅ Ти відправив `/start` боту
- ✅ Змінні оточення правильно налаштовані
- ✅ Сервер перезапущений після додавання змінних
